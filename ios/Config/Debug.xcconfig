#include "Shared.xcconfig"

// Local Express backend (cd backend && npm run dev), reached via the Mac's LAN IP so both
// the simulator AND a physical iPhone on the same Wi-Fi can connect. If your Mac's IP
// changes (check with `ipconfig getifaddr en0`), update it here.
API_BASE_URL = http:/$()/192.168.1.10:4000/api
